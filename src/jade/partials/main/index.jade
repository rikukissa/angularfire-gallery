div(header)

.file-thumbnails(infinite-scroll='loadMore()', infinite-scroll-immediate-check='false', infinite-scroll-distance='1')
  a(ng-href='#/files/{{ file.$id }}', ng-repeat='file in files')
    img.file-thumbnail.img-thumbnail(ng-src='{{ file.thumbnail }}')
